{
  "name": "project-skill-installer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "project-skill-installer": "dist/src/cli.js" },
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test dist/test/*.test.js",
    "test:catalog": "pnpm run build && node --test dist/test/catalog.test.js",
    "test:detector": "pnpm run build && node --test dist/test/detect.test.js"
  },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.3" }
}
